import logo from '../assets/logo.png';

/**
 * TODO(i18n): placeholder only. Replace hard-coded text with t() keys from
 * docs/COPY.md once src/i18n exists (ui-i18n rule: no hard-coded strings).
 */
export function DashboardApp() {
  return (
    <main>
      <img src={logo} alt="GDG on Campus" width={160} />
      <h1>Dashboard — coming soon</h1>
    </main>
  );
}
