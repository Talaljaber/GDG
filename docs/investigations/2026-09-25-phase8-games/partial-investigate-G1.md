# Partial results: G1, real-browser session (stopped before reporting)

Purpose: what track G1 (Opus) had done when the run was stopped. It built a Playwright spec for a How Many? / Swipe Sort / Pairs session with 3 phones and the host, including a reload mid-round, against the local stack, and had just started the web server. **No results were produced.** The spec and config are in `partial-state/g1/` (`g1.spec.ts`, `g1.config.ts`) and can be adapted into `e2e/` to rerun this track.

Last updated: 2026-09-25
